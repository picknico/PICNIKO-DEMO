# PICNIKO V15 — Messages Composer + Navigation Fix

- Bottom navigation on Messages starts minimized so it does not cover the message composer.
- A small `⌃ Menu` control remains visible at the bottom.
- Clicking it expands the navigation; clicking `⌄ Hide` minimizes it again.
- No database schema, RLS, QR, reward, or protected files were changed.
